<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bernal.jonatan.whip">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InfoPostAdoption"></activity>
        <activity android:name=".MostrarImatge" />
        <activity android:name=".UploadImageFirebase" />
        <activity android:name=".MostrarPerfil" />
        <activity android:name=".EditarPerfil" />
        <activity android:name=".InfoPost" />
        <activity android:name=".MisPosts" />
        <activity android:name=".NuevoPostPerdido" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_principal" />
        <activity android:name=".ListadoPerdida" />
    </application>

</manifest>